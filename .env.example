# Copy to .env and fill in real keys (.env is gitignored).
# 4 Groq API keys, comma-separated — the pool round-robins across them and
# rotates on rate limits. Secrets stay local; only the key index is ever logged.
GROQ_API_KEYS=key1,key2,key3,key4
